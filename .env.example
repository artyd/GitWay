# Рядок підключення до Postgres — обовʼязковий для збереження прогресу й рейтингу.
# Локально:      postgres://gitway:gitway@localhost:5432/gitway
# У docker compose застосунок отримує його автоматично (host = db).
DATABASE_URL=postgres://gitway:gitway@localhost:5432/gitway
